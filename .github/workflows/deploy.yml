# name: Deploy to lxp-cpu01 (retired — active deploy is in .gitlab-ci.yml deploy:lxp)

# on:
#   workflow_run:
#     workflows: [CI]
#     types: [completed]
#     branches: [main]

# jobs:
#   deploy:
#     if: github.event.workflow_run.conclusion == 'success'
#     runs-on: ubuntu-latest
#     timeout-minutes: 15
#     steps:
#       - name: Set up SSH
#         env:
#           SSH_KEY: ${{ secrets.N1_SSH_KEY }}
#           N1_HOST_KEY: ${{ secrets.N1_HOST_KEY }}
#         run: |
#           mkdir -p ~/.ssh
#           chmod 700 ~/.ssh
#           printf '%s\n' "$SSH_KEY" | tr -d '\r' > ~/.ssh/id_ed25519
#           chmod 600 ~/.ssh/id_ed25519
#           if [ -z "$N1_HOST_KEY" ]; then
#             echo "ERROR: N1_HOST_KEY secret is not set" >&2
#             exit 1
#           fi
#           printf '%s\n' "$N1_HOST_KEY" >> ~/.ssh/known_hosts
#           chmod 600 ~/.ssh/known_hosts

#       - name: Deploy
#         run: |
#           eval "$(ssh-agent -s)"
#           ssh-add ~/.ssh/id_ed25519
#           ssh -A -i ~/.ssh/id_ed25519 \
#             "${{ secrets.N1_USER }}@${{ secrets.N1_HOST }}" \
#             "set -e
#              mkdir -p ~/.ssh && chmod 700 ~/.ssh
#              ssh-keyscan -H github.com >> ~/.ssh/known_hosts
#              DEPLOY_PATH='${{ secrets.N1_DEPLOY_PATH }}'
#              REPO='git@github.com:MSKazemi/ExaMLOps-private.git'
#              if [ ! -d \"\$DEPLOY_PATH/.git\" ]; then
#                mkdir -p \"\$DEPLOY_PATH\"
#                GIT_TERMINAL_PROMPT=0 git clone \"\$REPO\" \"\$DEPLOY_PATH\"
#              fi
#              cd \"\$DEPLOY_PATH\"
#              GIT_TERMINAL_PROMPT=0 git pull origin main
#              docker compose -f platform/infra/docker-compose/docker-compose.yml up --build -d"
