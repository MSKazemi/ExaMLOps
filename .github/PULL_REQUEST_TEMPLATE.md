## What and why

<!-- The problem this solves and how. Link the issue: "Closes #123". -->

## Affected components

<!-- e.g. exa CLI, control plane, dashboard backend/frontend, serving, pipelines, Helm chart, docs -->

## How I verified it

<!-- The commands you ran and their result, e.g. `make gate` → green. -->

- [ ] `make test-fast` passes
- [ ] `make gate` passes (lint · format · typecheck · unit · strict docs build)
- [ ] Frontend changes: `npm test`, `npm run lint` and `npm run build` in `platform/services/dashboard/frontend/`

## Checklist

- [ ] Tests added or updated for the behaviour change
- [ ] Docs updated (guide, `--help` text, examples, command guide) where behaviour changed
- [ ] No concrete model or dataset named in `platform/` or `pipelines/` (use-case boundary)
- [ ] Database changes are additive (new tables/columns only)
- [ ] No secrets, internal hostnames or personal data in code, tests, docs or screenshots
- [ ] Commit messages follow Conventional Commits (`fix(serving): …`)

## Notes for reviewers

<!-- Migration or deployment impact, screenshots for UI changes, anything you are unsure about.
     If a substantial part was generated by an AI assistant, say so here. -->
