"""Dataset-side pipeline utilities (Next-Gen 40).

Currently hosts A1 dataset versioning (``versioning``). Kept a thin package so
the resolver can be imported without pulling in the heavy pipeline generator.
"""
