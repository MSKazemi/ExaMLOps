fastapi==0.115.12  # pinned exact: upgrade deliberately (aligned with the serving stack), never by float
uvicorn[standard]==0.34.2
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
httpx==0.28.1
pydantic-settings==2.9.1
aiosqlite==0.21.0
cryptography==45.0.4
PyJWT==2.10.1
alembic==1.13.2
aioboto3==13.1.1
PyYAML==6.0.2
python-multipart==0.0.20
pytest==8.3.5
pytest-asyncio==0.25.3
moto[s3]==5.2.2  # >=5.0.14 adds MockRawResponse.raw_headers for aiobotocore 2.13 compat
docker>=7.0
opentelemetry-distro==0.63b1
opentelemetry-exporter-otlp-proto-grpc==1.42.1
opentelemetry-instrumentation-fastapi==0.63b1
opentelemetry-instrumentation-httpx==0.63b1
# CLI Console (ADR 0119): the dashboard runs the real `exa` from the bind-mounted source
# (PYTHONPATH=/repo/platform/cli/src). These are the examlops CLI's own runtime dependencies —
# without them `python -m examlops.cli` dies on import and every console run fails. Pinned to
# the versions the root workspace venv runs the CLI with.
typer==0.25.1
click==8.4.0
rich==15.0.0
tomli-w==1.2.0
prometheus-client==0.25.0
