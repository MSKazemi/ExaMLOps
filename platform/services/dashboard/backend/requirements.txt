fastapi==0.141.1  # pinned exact: upgrade deliberately (aligned with the serving stack), never by float
uvicorn[standard]==0.53.0
sqlalchemy[asyncio]==2.0.53
asyncpg==0.31.0
httpx==0.28.1
pydantic-settings==2.15.0
aiosqlite==0.22.1
cryptography==50.0.1
# The live stream's NATS backbone bridge (backbone.py, ADR 0124); unused unless
# EXAMLOPS_EVENT_PUBLISHER=nats. Same pin as uv.lock's examlops[events].
nats-py==2.15.0
PyJWT==2.14.0
alembic==1.20.0
aioboto3==15.5.0
PyYAML==6.0.3
python-multipart==0.0.32
pytest==9.1.1
pytest-asyncio==1.4.0
moto[s3]==5.2.3  # >=5.0.14 adds MockRawResponse.raw_headers for aiobotocore 2.13 compat
docker>=7.2.0
opentelemetry-distro==0.65b0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
# CLI Console (ADR 0119): the dashboard runs the real `exa` from the bind-mounted source
# (PYTHONPATH=/repo/platform/cli/src). These are the examlops CLI's own runtime dependencies —
# without them `python -m examlops.cli` dies on import and every console run fails. Pinned to
# the versions the root workspace venv runs the CLI with.
typer==0.27.2
click==8.5.0
rich==15.0.0
tomli-w==1.2.0
prometheus-client==0.26.0
